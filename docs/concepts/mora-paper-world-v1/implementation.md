# Mora paper-world implementation — 19 September 2026

Two fresh built-in-generated Observatory sources are active: landscape1536×1024 and portrait1024×1536. Exact prompts are in [prompts.md](prompts.md); UI texture prompt is in [ui-texture.md](ui-texture.md). Runtime geometry is authoritative in `lib/games/observatory-art.json` and `lib/games/observatory-portrait-art.json`. The JSON files beside this note retain the initial image measurements; runtime also includes full-landmark overview crops.

`paperWorldFrame` fits the union of the gameplay crop and complete landmarks inside the fixed available stage. Portrait swaps both source and placement coordinates, and fits without board scrolling. It does not squeeze the landscape composition. Source pixels have one uniform scale. The generated images missed the requested protected regions; fitting actual measured bounds preserves complete roofs and bases. Some aspect ratios expose the neutral paper table outside the illustration, deliberately prioritizing complete gameplay over cropping landmarks. Finite native resolution remains visible on large displays.

The canvas renders two measured crown regions with subtle local rotation and clipped pond ripples, leaving buildings, pads, targets and text stationary. Rendering is capped at25fps, DPR2 and6million pixels; it suspends offscreen/in hidden tabs, releases its frame/listeners on unmount, and uses a static image under reduced motion or loading failure. Sound is unchanged.

The new UI texture supplies real fibrous paper grain to folded trays, field tags, leaf permits, stitched migration slips and goal stamps. Scores use dark ink on cream patches. Die faces depict square/circle pads, stone paving and timber; no habitat glyph rows remain. All four static die memberships and state-dependent restrictions are unchanged. Eligible habitats receive a dashed outline before card selection; final placement still uses the engine's complete legality check. The existing Floodline artwork is preserved; these new board images apply to Observatory.

Advanced inspection preserves the live camera, omits the duplicated self board, shows a passing-order strip anchored at You, and includes every opponent's goals and remaining extension uses. Shared player state icons communicate choosing/locked/waiting and die roller with accessible names and static reduced-motion shapes. Portrait navigation, player badges, goals, board, actions and hand have independent reserved areas; action controls no longer use a vertical scroll column.

Validation:101 tests pass, including hidden simultaneous decisions and extension atomicity; TypeScript, lint and production build pass. Full suite and prerender required local loopback permission. Static artwork/target previews in this directory show occupied pads and reserved UI bands, not rendered browser screenshots. New geometry tests exercise phone/tablet/desktop/fractional-zoom viewports, all17 placements, complete landmarks and source alignment. Browser interaction, actual phone touch, live animation appearance and keyboard/focus behavior have not been checked. The smallest portrait phones necessarily display smaller creatures; actual usability needs device review.

The shared skill was updated. Its Python validator could not run because the default Python lacks PyYAML; frontmatter and references were checked directly. No commit, push or deployment.
